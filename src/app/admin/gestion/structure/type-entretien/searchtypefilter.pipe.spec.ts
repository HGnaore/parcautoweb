import { SearchtypefilterPipe } from './searchtypefilter.pipe';

describe('SearchtypefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtypefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
