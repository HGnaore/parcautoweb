import { SearchgarfilterPipe } from './searchgarfilter.pipe';

describe('SearchgarfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchgarfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
